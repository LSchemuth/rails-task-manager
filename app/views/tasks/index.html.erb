<h1>All Tasks</h1>

<ul>
<% @tasks.each do |task| %>
  <li>
    <%= link_to task.title, task_path(task) %> |
    <%= link_to "X", task_path(task), method: :delete %>
  </li>
<% end %>
</ul>


<%= link_to "New task" , tasks_new_path(@task) %>
